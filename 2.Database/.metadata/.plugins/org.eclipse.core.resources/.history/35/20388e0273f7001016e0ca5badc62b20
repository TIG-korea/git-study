SELECT emp_no '사번', salary / 12 AS '월급' FROM salaries s ;
SELECT * FROM employees;
SELECT emp_no + 50, first_name FROM employees
WHERE first_name = '길동 ';


SELECT DISTINCT gender FROM employees; -- 해당하는 컬럼에 중복된 값은 빼고서 결과값이 나옴  (테이블명 + 컬럼명)

-- salaries 테이블에서 salary 컬럼의 값을 월급이라고 가정 
-- 1. 월급 (기존값)
-- 2. 예상 연봉 (salary * 12 ) 별칭은 "예상 연봉"
-- 3. 5% 인상 후의 예상 연봉 (salary * 12 * 1.05 ) 별칭은 " 인상 후 예상 연봉" 

SELECT * FROM salaries;

SELECT salary * 12 "예상 연봉", salary * 12 * 1.05 "인상 후 예상 연봉" FROM salaries;

SELECT emp_no, salary, from_date, to_date FROM salaries
WHERE salary >= 80000;


